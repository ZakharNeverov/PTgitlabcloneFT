#ifndef STRINGS_PARSE_METHODS_HPP
#define STRINGS_PARSE_METHODS_HPP

#include <string>
#include <vector>

namespace bokov
{
  std::vector < std::string > getRowOfWords(std::string& str);
  std::string getSubString(std::string& str);
}
#endif
